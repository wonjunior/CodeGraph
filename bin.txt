// nodes.n12.root = nodes.n8.root
// nodes.n1.root = nodes.n2.root
// nodes.n2.root.node = nodes.n12.root.node

/* Intersect

Object.prototype.intersect = function(arr) {

	return Object.entries(arr).reduce((intersection, [key, value]) => {

		if (!this.hasOwnProperty(key)) return intersection;

		return { ...intersection, ...{[key]: value} };

	}, {});

}

*/
