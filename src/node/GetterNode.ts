import Graph from '@/Graph'
import GetterProcess from '@/interpreter/process/GetterProcess'
import Node from '@/node/Node'

export default class GetterNode extends Node {
    // constructor(graph: Graph, args) { //? ??
    //     super(new GetterProcess('a'), null, graph, args) //? wait wait wait how do you know it's "a"?
    // }
}