/*
** Class designed for DOM manipulation
*/
/*class Element {

    constructor(array) {
        this.array = array;
    }

    appendTo(element) {
        return $(this.array.join('')).appendTo(element);
    }

    drawSVG() {
        $('svg').append(this.array).html($('svg').html());
    }

} let element = (array) => new Element(array);

class Editor {

    constructor() {
        this.objects = $('.objects');

    }

}; let editor = new Editor()

class UI {

    static select(node) {

        $('.container#selected').removeAttr('id');
        node.attr('id', 'selected')

    }

}*/
